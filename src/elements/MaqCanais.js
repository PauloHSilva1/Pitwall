import React, { useState } from 'react';
import { Button, Typography } from '@mui/material';
import { styled } from '@mui/system';

function MaqCanais(){
    return(
        <div>
            <Typography variant='h4'>Selecionar configuração</Typography>
           
        </div>
    )
}

export default MaqCanais